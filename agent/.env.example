# AgenticHire — Agent Worker
# NOTE: This folder is NO LONGER NEEDED when using ElevenLabs Conversational AI.
# The AI interview agent now runs on ElevenLabs' hosted infrastructure.
# Configure your agent at: https://elevenlabs.io/app/conversational-ai
#
# The system prompt from agent/src/prompts/systemPrompt.ts should be
# copied into your ElevenLabs agent configuration.
#
# Server-side tools (fetchContext, fetchQuestion, concludeInterview) are
# now called by ElevenLabs as webhooks to the /api/agent/* endpoints.

# ── Legacy config (kept for reference) ───────────────────────────

# API Server
API_BASE_URL=http://localhost:5000/api

# Internal agent API key (must match server's AGENT_INTERNAL_API_KEY)
AGENT_INTERNAL_API_KEY=your_internal_key_here
